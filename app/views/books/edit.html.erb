<h1>Editing Book</h1>
<h4>Title</h4>
<%= form_with model: @book do |f| %>
        <%= f.text_field :title %>

        <h4>Opinion</h4>
        <%= f.text_area :body %>
        <nav>

        <%= f.submit 'Update Book' %>
<% end %>
        <br>
        <%= link_to "Show",book_path %>

         <%= link_to "Back", books_path %>
        </br>